
<div class="container">
{{#each posts}}
<div class="container border mb-5">

    <div class="row border-bottom pl-3 pr-3">
<div>{{author}}</div><div class="d-flex ml-auto">{{created_at}}</div>
    </div>

<div class="row border-bottom justify-content-center"><h3 class="mb-0">{{post_title}}</h3></div>
<div class="row pt-2 pb-2 pl-3 pr-3">{{post_content}}</div>
</div>
{{/each}}
</div>